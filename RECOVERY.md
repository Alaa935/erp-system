# دليل استعادة البيانات — Recovery Guide

## حالات الفشل المحتملة

### 1. تلف IndexedDB (Corruption)

**الأعراض:**
- الخطأ: `DatabaseClosedError`, `InternalError`, `AbortError`
- الصفحة لا تفتح أو تظهر شاشة بيضاء
- البيانات تظهر ناقصة

**الإصلاح:**

#### الطريقة 1 — إعادة تحميل الصفحة
```js
// في Console المتصفح
window.location.reload();
```

#### الطريقة 2 — مسح IndexedDB (فقدان بيانات)
```js
// في Console المتصفح
// 1. احذف قاعدة البيانات
indexedDB.deleteDatabase('WMSDatabase');
// 2. امسح localStorage
localStorage.clear();
// 3. أعد تحميل الصفحة — سيتم إعادة تعبئة البيانات الافتراضية
window.location.reload();
```

#### الطريقة 3 — استعادة من نسخة احتياطية
1. اذهب إلى Settings → Backup
2. استخدم زر "استعادة من ملف نسخة احتياطية"
3. اختر ملف JSON الذي سبق أن صدّرته

### 2. فقدان بيانات localStorage (Session)

**الأعراض:**
- طلب تسجيل دخول مفاجئ
- اختفاء الإعدادات

**الإصلاح:**
```js
// في Console المتصفح
// localStorage ليس له علاقة بالبيانات — فقط بالجلسة والإعدادات
// سجّل الدخول مرة أخرى
// إذا كانت الإعدادات مفقودة، اذهب إلى Settings → General وأعد إدخالها
```

### 3. متصفح لا يدعم IndexedDB

**الأعراض:**
- الخطأ: `DataError`, `InvalidStateError`
- الصفحة لا تعمل

**الحل:**
- استخدم متصفح حديث (Chrome 80+, Firefox 75+, Edge 80+)
- تأكد من عدم استخدام الخصوصية الكاملة (Incognito قد يمنع IndexedDB)

### 4. تجاوز حدود التخزين

**الأعراض:**
- الخطأ: `QuotaExceededError`
- عدم القدرة على إضافة بيانات جديدة

**حدود IndexedDB التقريبية:**
- Chrome: 80% من مساحة القرص (60% لـ PWA)
- Firefox: 2GB (بعد الموافقة)
- Edge: 80% من مساحة القرص

**الإصلاح:**
- صدّر نسخة احتياطية
- استخدم "تصفير العمليات" في Settings → Backup
- أعد استيراد البيانات النظيفة
- أو امسح بيانات الموقع: إعدادات المتصفح → الخصوصية ← بيانات المواقع ← امسح

## الوقاية

### نوصي بتنفيذ هذه الممارسات:

1. **نسخ احتياطي أسبوعي** — اذهب إلى Settings → Backup → "تحميل النسخة الاحتياطية"
2. **تصدير السجل** — اذهب إلى Settings → Activity ← "تصدير السجل"
3. **لا تمسح بيانات المتصفح دون نسخ احتياطي**

### في حالة الاشتباه بمشكلة:

1. **لا تمسح أي شيء** — أولاً، جرب تصدير البيانات
2. استخدم `exportAllData()` في Console المتصفح:
   ```js
   const { db } = await import('./db/db');
   // هذا لن يعمل مباشرة في Console — استخدم واجهة Settings
   ```
3. إذا فشل التصدير، جرب استعادة صفحة سابقة

## إعادة تعيين كامل (Nuclear Option)

إذا تلفت البيانات بشكل كامل ولا يمكن استعادتها:

```js
// في Console المتصفح
indexedDB.deleteDatabase('WMSDatabase');
localStorage.clear();
localStorage.setItem('disableSeeding', 'false');
window.location.reload();
```

⚠️ **تحذير:** هذا الخيار يمسح كل البيانات بشكل دائم. استخدمه فقط كحل أخير.
